import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  components: {
    Card: {
      paddingLG: 8,
    },
  },
  token: {
    colorPrimary: '#a5aa60',
  },
};

export default theme;
