'use client';
import { Typography } from 'antd';

const { Title } = Typography;

const Home = () => {
  return (
    <div>
      <Title level={4}>Well come to woo tool</Title>
    </div>
  );
};

export default Home;
